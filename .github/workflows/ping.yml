name: Ping URL

on:
  schedule:
    - cron: "*/14 * * * *"   # every 14 minutes
  workflow_dispatch:         # allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl the website
        run: |
          curl -I https://talkateave.onrender.com || echo "Request failed"
